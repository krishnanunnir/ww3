from django.shortcuts import render
from modules.hackerearth import *

# Create your views here.
def submit_code(request):
    if request.method == "POST" :
        
    else:

def receive_earth(request):
    if request.method == "POST":
        
    else:
        return redirect('/')